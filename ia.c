#include "lemipc.h"

void		move(t_ia *ia, t_map *map)
{
  //change_case();
  if (ia == NULL)
    printf("lel");
  if (map == NULL)
    printf("ce code est la pur compiler avec les flags");
}

void		war(t_map *map, t_ia *ia)
{
  if (ia == NULL)
    printf("lel");
  if (map == NULL)
    printf("ce code est la pur compiler avec les flags"); 
}
